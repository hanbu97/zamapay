{
  "name": "zamapay-install-shape-esbuild",
  "private": true,
  "type": "module",
  "dependencies": {
    "@zamapay/server": "file:../.."
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node18 --format=esm --minify --outfile=dist/index.js",
    "runtestproject": "npm run build && node dist/index.js"
  }
}
