# Implementation Status Audit

## Objective

Implement the Mermer Pay hackathon demo: Rust backend, latest Next.js frontend, shadcn commercial dashboard UI, Zama FHEVM identity/payment integration, merchant payment platform flow, and a card issuing demo that can prove the payment rail locally and later on Sepolia.

## Prompt-to-Artifact Audit

| Prompt requirement | Concrete artifact | Current evidence | Gap |
| --- | --- | --- | --- |
| Rust backend | `crates/api`, `crates/domain`, `crates/indexer`, `crates/fulfillment`, `crates/shared`, `crates/storage` | `npm run verify:local:full` passes `cargo fmt --all --check` and `cargo test --workspace`. | None locally |
| Latest Next.js frontend | `apps/web/package.json`, `apps/web/app`, `apps/web/components` | `apps/web/package.json` uses Next `^16.2.4`, React `^19.2.3`, Tailwind `^4.2.4`; `npm run verify:local:full` passes Web unit tests, live Web e2e tests, typecheck, and production build. | None locally |
| Latest shadcn commercial/admin UI | `apps/web/components.json`, `apps/web/components/ui`, shell/login/dashboard/ops/checkout/card-store pages | `npx shadcn@latest info --json` reports `base-nova`; `npx shadcn@latest add card item table tabs sidebar field badge button alert progress separator --dry-run` reports 17/17 identical; Playwright screenshots cover home, login, and ops desktop/mobile, including `mermer-login-shadcn-session-scope-20260507.png`. | None locally |
| Zama-backed identity/login foundation | `crates/api/tests/auth_nonce_session.rs`, `apps/web/components/auth/LoginCard.tsx`, `scripts/local-readiness.js` | `npm run verify:local:full` passes local wallet nonce/signature session proof and protected dashboard render. | Public wallet Sepolia login still needs live browser run |
| epusdt-style payment lifecycle implemented with Zama refs | `contracts/contracts/*`, `contracts/scripts/smoke-local-invoice.js`, `contracts/scripts/project-finalized-payment.js`, `apps/web/lib/fhevm.ts`, `apps/web/components/checkout/CheckoutPaymentCard.tsx` | Contract tests and local smoke prove invoice create, encrypted approval/payment, public decrypt, finalize payment, Rust projection, and finality-safe fulfillment. | Real Zama Sepolia relayer path not executed |
| Complete merchant payment platform shell | `/`, `/login`, `/dashboard`, `/checkout/[invoiceId]`, `/ops` | Local readiness loads homepage, login, protected dashboard, checkout artifact page; Playwright screenshots show shadcn layout and mobile ops readability. `operator-failure-drills.spec.ts` proves `/ops` renders expired invoice, webhook dead-letter, rollback, decrypt, replay, reorg, and frozen fulfillment state from Rust diagnostics. | Public demo credentials and deployment missing |
| Test project example: card issuing demo | `demo/cardforge`, checkout artifact rendering, fulfillment release audit | CardForge is now an independent workspace configured by Mermer Pay API/checkout/project env values; `npm run verify:local:full` still proves the local checkout artifact release path with three card-code artifacts and one release job. | Same scenario not yet run on Sepolia |
| Testnet proof | `docs/runbooks/sepolia-demo.md`, `docs/runbooks/testnet-config.md`, `contracts/scripts/verify-sepolia-readiness.js`, `generated/contracts/addresses/sepolia.json` | `npm run deploy:sepolia` deployed all three contracts, `npm run verify:sepolia` passed at block `10804469`, and `npm run mint:test-usd:sepolia` minted buyer test USD in tx `0x5c7838ce7a110e687cdc282cd7e50c50aa06cdf89871cf29bc649644dd6d0483`. | Browser wallet checkout still pending |

## Checklist

| Requirement | Artifact | Current evidence | Status |
| --- | --- | --- | --- |
| Rust backend | `crates/api`, `crates/domain`, `crates/indexer`, `crates/fulfillment`, `crates/shared`, `crates/storage` | `cargo fmt --all --check` and `cargo test --workspace` passed on 2026-05-07 with auth, invoice, manifest, projection, finality, signed webhook, exactly-once fulfillment release, and portal persistence tests green. | Done locally |
| Next.js latest frontend | `apps/web/package.json`, `apps/web/app/*`, `apps/web/components/*`, `apps/web/tests/*` | `npx shadcn@latest info --json` reports Next.js `16.2.4`, Tailwind `v4`, RSC enabled; `npm run verify:local:full` passed on 2026-05-06 and includes `npm run test:web`, `npm run lint:web`, and `npm run build:web`. | Done locally |
| shadcn commercial UI | `apps/web/components.json`, `apps/web/components/ui/*`, homepage, login, dashboard, checkout, card-store, and ops pages | shadcn `base-nova`, neutral theme, lucide icons; installed components include `alert`, `avatar`, `badge`, `breadcrumb`, `button`, `card`, `dropdown-menu`, `empty`, `field`, `input-group`, `progress`, `select`, `sheet`, `sidebar`, `table`, `tabs`, `textarea`, and `tooltip`. `npx shadcn@latest add card item table tabs sidebar field badge button alert progress separator --dry-run` reports 17/17 files identical. Playwright screenshots include `mermer-current-home-audit-20260507.png`, `mermer-login-shadcn-session-scope-20260507.png`, `mermer-ops-expanded-failure-drill-20260507.png`, and `mermer-current-ops-mobile-audit-20260507.png`; mobile queue text wraps instead of truncating. | Done locally |
| Wallet login | `crates/api/src/lib.rs`, `crates/api/tests/auth_nonce_session.rs`, `apps/web/components/auth/LoginCard.tsx`, `apps/web/e2e/auth-login.spec.ts`, `apps/web/app/api/dev/sign-message/route.ts`, `apps/web/lib/dev-signer-gate.ts`, `apps/web/tests/dev-signer-gate.test.ts`, `scripts/local-readiness.js` | Nonce/signature session test passes. Latest `npm --prefix apps/web run test:e2e -- auth-login.spec.ts` verifies anonymous dashboard redirect, Rust nonce signing, `mermer_session` issuance, `/api/session`, and authenticated dashboard render. `npm run verify:local:full` includes full Web e2e. `npm run test:web` covers explicit opt-in, no opt-in, production, Sepolia, and remote-host rejection. | Done locally |
| Zama FHEVM payment core | `contracts/contracts/ConfidentialInvoiceSettlement.sol`, `contracts/contracts/ConfidentialUSDMock.sol`, `apps/web/lib/fhevm.ts`, `apps/web/components/checkout/CheckoutPaymentCard.tsx` | `npm run verify:local:full` passed on 2026-05-06 and includes contract tests for exact encrypted payment, underpayment rejection, duplicate rejection, and expiry rejection. Local smoke proves encrypted approval, encrypted payment, public decrypt, and `finalizePayment`. | Done locally |
| Merchant payment platform | `apps/web/app/dashboard/page.tsx`, `apps/web/components/dashboard/CreateInvoiceForm.tsx`, `apps/web/lib/merchant-checkout.ts`, `crates/api/src/lib.rs`, `crates/storage/src/lib.rs` | Dashboard creates hosted checkout through wallet-owned contract writes, projects tx metadata to Rust, and redirects to checkout. Rust keeps auth sessions process-local, while `DATABASE_URL` persists portal projects, API keys, checkout sessions, invoices, projections, webhook state, subscriptions, and withdrawals in normalized Postgres tables as the single portal source of truth. | Done locally |
| Wallet chain safety | `apps/web/lib/wallet.ts`, `apps/web/lib/merchant-checkout.ts`, `apps/web/components/checkout/CheckoutPaymentCard.tsx` | Browser merchant checkout now switches/adds local chain `31337` or Sepolia before writes; buyer payment switches/adds Sepolia before encrypted approval/payment/finalization. `npm run lint:web` and `npm run build` passed after the change. | Done locally |
| Browser-to-backend payment projection | `apps/web/app/api/checkout/project-finalized-payment/route.ts`, `apps/web/components/checkout/CheckoutPaymentCard.tsx` | Checkout now submits only the finalization tx hash to a server route. The route verifies `InvoicePaid` from the current settlement contract before calling Rust operator projection and confirmations. Latest `npm run verify:local` projected tx `0x591c510fe709f0cfbfc52c7c5d326071cc8b1e05d505b3e162ef206fe95996ca` through the web route and returned `paid`, `finality_safe`, and `ready`. | Done locally |
| Merchant decrypt view | `contracts/contracts/ConfidentialInvoiceSettlement.sol`, `contracts/test/confidential-invoice-settlement.js`, `apps/web/lib/fhevm.ts`, `apps/web/components/dashboard/SettlementDecryptCard.tsx`, `apps/web/app/dashboard/page.tsx` | Contract tests prove merchant and payout wallet can decrypt settled amount while outsider is rejected. Dashboard now has a `Decrypt` tab that reads the encrypted settlement handle and uses wallet EIP-712 authorization plus Zama relayer `userDecrypt` on Sepolia; local mode stays locked rather than inventing plaintext. `npm run lint:web`, `npm run build:web`, and screenshot `mermer-dashboard-decrypt-tab.png` passed on 2026-05-07. | Local ACL done, Sepolia browser decrypt pending |
| Operator diagnostics | `crates/domain/src/payment.rs`, `crates/indexer/src/lib.rs`, `crates/api/src/lib.rs`, `crates/shared/src/lib.rs`, `crates/storage/src/lib.rs`, `apps/web/lib/operator.ts`, `apps/web/app/ops/page.tsx`, `apps/web/e2e/operator-failure-drills.spec.ts` | Added one operator settlement-event projection path for expired invoice, decrypt timeout, replay guard, rollback, and deep reorg states. Webhook delivery has a domain retry/dead-letter state machine, an operator-key-protected signed dispatch route, diagnostics counters for expired invoices plus pending/retrying/dead-letter queues, indexer cursor and stalled exposure, persisted finality confirmations/threshold on invoice records, and `/ops` invoice-level status display. Merchant decrypt requests reject duplicate pending work, gateway callbacks require `MERMER_GATEWAY_CALLBACK_KEY`, replay callbacks are ignored without corrupting state, completed callbacks no longer count as pending decrypt jobs, and operator-key failures are counted before 401 then exposed through authorized diagnostics. Latest `npm --prefix apps/web run test:e2e -- operator-failure-drills.spec.ts` passed on 2026-05-07 and scripts expired invoice, webhook dead-letter, rollback before threshold, decrypt timeout, replay guard, deep reorg, frozen fulfillment, duplicate decrypt-request guard, operator auth rejection, indexer-stalled cursor, finality depth, and `/ops` incident rendering. `cargo fmt --all --check` and `cargo test --workspace` passed on 2026-05-07, including indexer expiry/paid-truth regression tests plus `operator_webhook_delivery_retries_dead_letters_and_recovers` and `merchant_decrypt_request_rejects_duplicates_and_gateway_replay`. | Done locally |
| epusdt-style payment logic reference outcome | `contracts/scripts/smoke-local-invoice.js`, `contracts/scripts/project-finalized-payment.js` | Payment lifecycle has deterministic invoice creation, payment submission, finalization, operator projection, finality advancement, and fulfillment release. | Done locally |
| Card issuing demo | `demo/cardforge`, `apps/web/app/checkout/[invoiceId]/page.tsx`, `apps/web/e2e/checkout-flow.spec.ts` | CardForge is a standalone merchant demo project and calls Mermer Pay through configured HTTP/checkout URLs. Latest checkout e2e runs the local confidential payment smoke, posts the finalization tx through the web projection route, then renders `/checkout/{invoiceId}` with finality depth, all three card-code artifacts, and fulfillment job id. `npm run verify:local:full` includes full Web e2e. | Done locally |
| Local end-to-end proof | `contracts/scripts/smoke-local-invoice.js`, `apps/web/e2e/*.spec.ts`, `scripts/local-readiness.js`, `scripts/local-full-verify.js` | Latest `npm run verify:local:full` passed on 2026-05-07. It covered Web unit tests, live Web e2e tests including auth, checkout, and expanded operator failure drills, Web typecheck, Web production build, Rust format, Rust workspace tests, contract tests, and local end-to-end readiness. The latest separate `npm run verify:local` also proved local manifest/API/web/login/dev-signer-boundary checks, confidential local payment tx `0xcb2cba7f8fe2f298ffed8b691a067b30736dba4a291b8071473a08b1440f1e3e`, paid/finality-safe settlement, persistent `fulfillmentRelease`, three artifacts, and checkout artifact rendering. | Done locally |
| Sepolia deployment readiness | `contracts/scripts/deploy-contracts.js`, `contracts/scripts/verify-sepolia-readiness.js`, `contracts/scripts/public-hardhat-keys.js`, `generated/contracts/addresses/sepolia.json`, `docs/runbooks/sepolia-demo.md`, `docs/runbooks/testnet-config.md` | `npm run deploy:sepolia` succeeded on 2026-05-07 and wrote `MerchantRegistry=0xD3E6345A856a4339effe3cf128745db2CAa4D2d1`, `ConfidentialUSDMock=0x473FBc0B9761F1E879c22971D9b77134f03C42bE`, and `ConfidentialInvoiceSettlement=0x6975Bba2F12F47a49028120C6c51fBc9D2Fd6015`. After restarting Rust API, `npm run verify:sepolia` passed at Sepolia block `10804469`, including deployer balance, buyer balance, manifest parsing, deployed bytecode, non-default secrets, and Rust-served Sepolia manifest. | Done |
| Buyer test liquidity on Sepolia | `contracts/scripts/mint-test-usd.js`, `contracts/scripts/public-hardhat-keys.js` | `npm run mint:test-usd:sepolia` succeeded for buyer `0xC431773Fbc13B36384077847B884dE5D8dB91618`, amount `1000000000`, tx `0x5c7838ce7a110e687cdc282cd7e50c50aa06cdf89871cf29bc649644dd6d0483`. | Done |

## Required Evidence Before Claiming Complete

The project is not complete until these public-network checks pass:

1. Done: `DEPLOYER_PRIVATE_KEY` is set for a funded, non-public Sepolia deployer wallet in repo-root `.env`.
2. Done: `npm run deploy:sepolia` wrote `generated/contracts/addresses/sepolia.json`.
3. Done: `MERMER_OPERATOR_KEY` is set to a non-default value shared by the Rust API, Next server route, and operator scripts.
4. Done: `MERMER_WEBHOOK_SECRET` is set to a non-default value for signed merchant webhooks.
5. Done: `MERMER_GATEWAY_CALLBACK_KEY` is set to a non-default value for Zama gateway decrypt callbacks.
6. Done: `npm run verify:sepolia` passes every readiness check.
7. Done: `npm run mint:test-usd:sepolia` returned Sepolia mint tx `0x5c7838ce7a110e687cdc282cd7e50c50aa06cdf89871cf29bc649644dd6d0483`.
8. Done: The web app runs with `NEXT_PUBLIC_CONTRACT_ENV=sepolia` at `http://127.0.0.1:3001`; browser evidence is `/Users/hanbu/MyProjects/hackerthon/zama/predict/mermer-sepolia-card-store-ready-20260507.png`.
9. Pending: A real browser wallet completes `Mermer Pay login/project config -> CardForge standalone app -> /checkout/{invoiceId}`.
10. Pending: Checkout displays a `Finalization transaction` hash after encrypted approval, encrypted payment, public decrypt, and `finalizePayment`.
11. Pending: `/api/checkout/project-finalized-payment` verifies that Sepolia transaction and advances the invoice to `paid` and `finality_safe`; `project:payment:sepolia` is only the fallback.
12. Pending: Refreshing checkout reveals the three card-code artifacts.

## Current Blocker

External faucet funding is resolved, Sepolia deployment is live, and buyer test USD is minted. The remaining public-network blocker is browser-wallet execution: import or select the buyer wallet, run the web app with `NEXT_PUBLIC_CONTRACT_ENV=sepolia`, and complete the hosted checkout through the Zama relayer flow.
